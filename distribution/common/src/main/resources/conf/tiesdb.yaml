# TiesDB config YAML
#saveSettingsOnStop: true
config:
  TEST: !!TiesService
    transports:
      - !!WebSocketTransport
        #handler: !!JsonHandler
        #  charset: UTF-8
        handler: !!HandlerV0R0
        workerThreadsCount: 8
        serviceAddress: localhost
        servicePort: 8080
        #security:
        #  securedSocket: false
